# Default server for port 80
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;

    location / {
        return 200 '<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Better NPM - Proxy Manager</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 20px;
            padding: 60px 40px;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            text-align: center;
        }
        .logo {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            font-size: 40px;
            color: white;
            font-weight: bold;
        }
        h1 {
            font-size: 32px;
            color: #1a202c;
            margin-bottom: 16px;
            font-weight: 700;
        }
        p {
            font-size: 18px;
            color: #718096;
            margin-bottom: 40px;
            line-height: 1.6;
        }
        .status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: #f0fdf4;
            color: #166534;
            padding: 12px 24px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
            margin-bottom: 30px;
        }
        .status::before {
            content: "";
            width: 8px;
            height: 8px;
            background: #16a34a;
            border-radius: 50%;
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        .info {
            background: #f8fafc;
            border-radius: 12px;
            padding: 24px;
            margin-top: 30px;
        }
        .info-item {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #e2e8f0;
        }
        .info-item:last-child {
            border-bottom: none;
        }
        .info-label {
            color: #64748b;
            font-size: 14px;
        }
        .info-value {
            color: #1e293b;
            font-weight: 600;
            font-size: 14px;
        }
        .footer {
            margin-top: 40px;
            color: #94a3b8;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">🌐</div>
        <h1>Better NPM</h1>
        <p>프록시 관리 시스템이 정상적으로 작동 중입니다</p>
        
        <div class="status">
            시스템 정상 작동 중
        </div>

        <div class="info">
            <div class="info-item">
                <span class="info-label">서비스</span>
                <span class="info-value">Nginx Proxy Manager</span>
            </div>
            <div class="info-item">
                <span class="info-label">버전</span>
                <span class="info-value">Better NPM v1.0</span>
            </div>
            <div class="info-item">
                <span class="info-label">상태</span>
                <span class="info-value" style="color: #16a34a;">✓ 실행 중</span>
            </div>
        </div>

        <div class="footer">
            <p>프록시 호스트를 설정하면 이 페이지가 표시되지 않습니다</p>
        </div>
    </div>
</body>
</html>';
        add_header Content-Type text/html;
    }
}

# Default server for port 443 (SSL)
server {
    listen 443 ssl default_server;
    listen [::]:443 ssl default_server;
    http2 on;
    http3 on;
    quic_retry on;
    server_name _;

    # Self-signed certificate for default server
    ssl_certificate /data/nginx/default_cert.pem;
    ssl_certificate_key /data/nginx/default_cert.key;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        return 200 '<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Better NPM - Proxy Manager (HTTPS)</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 20px;
            padding: 60px 40px;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            text-align: center;
        }
        .logo {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 30px;
            font-size: 40px;
            color: white;
            font-weight: bold;
        }
        h1 {
            font-size: 32px;
            color: #1a202c;
            margin-bottom: 16px;
            font-weight: 700;
        }
        .https-badge {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            background: #dbeafe;
            color: #1e40af;
            padding: 8px 16px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 13px;
            margin-bottom: 20px;
        }
        p {
            font-size: 18px;
            color: #718096;
            margin-bottom: 40px;
            line-height: 1.6;
        }
        .status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            background: #f0fdf4;
            color: #166534;
            padding: 12px 24px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 14px;
            margin-bottom: 30px;
        }
        .status::before {
            content: "";
            width: 8px;
            height: 8px;
            background: #16a34a;
            border-radius: 50%;
            animation: pulse 2s infinite;
        }
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }
        .info {
            background: #f8fafc;
            border-radius: 12px;
            padding: 24px;
            margin-top: 30px;
        }
        .info-item {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #e2e8f0;
        }
        .info-item:last-child {
            border-bottom: none;
        }
        .info-label {
            color: #64748b;
            font-size: 14px;
        }
        .info-value {
            color: #1e293b;
            font-weight: 600;
            font-size: 14px;
        }
        .footer {
            margin-top: 40px;
            color: #94a3b8;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">🔒</div>
        <h1>Better NPM</h1>
        <div class="https-badge">🔐 HTTPS 연결</div>
        <p>보안 연결로 프록시 관리 시스템에 접속하셨습니다</p>
        
        <div class="status">
            시스템 정상 작동 중
        </div>

        <div class="info">
            <div class="info-item">
                <span class="info-label">서비스</span>
                <span class="info-value">Nginx Proxy Manager</span>
            </div>
            <div class="info-item">
                <span class="info-label">버전</span>
                <span class="info-value">Better NPM v1.0</span>
            </div>
            <div class="info-item">
                <span class="info-label">프로토콜</span>
                <span class="info-value">HTTP/3 + QUIC</span>
            </div>
            <div class="info-item">
                <span class="info-label">상태</span>
                <span class="info-value" style="color: #16a34a;">✓ 실행 중</span>
            </div>
        </div>

        <div class="footer">
            <p>프록시 호스트를 설정하면 이 페이지가 표시되지 않습니다</p>
        </div>
    </div>
</body>
</html>';
        add_header Content-Type text/html;
    }
}
